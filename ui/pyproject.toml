[project]
name = "rudebot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.1",
    "streamlit>=1.48.1",
    "requests"
]
[tool.uv]